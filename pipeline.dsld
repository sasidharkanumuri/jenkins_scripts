$job_name='newLoggingSample'
pipelineJob($job_name) 
{
 logRotator(1,2,3,4)
 wrappers{
 colorizeOutput('xterm')
 }
  definition {
  cpsScm {
 scm {
        git('git@git') { node -> // is hudson.plugins.git.GitSCM
            node / gitConfigName('sasidharkanumuri')
            node / gitConfigEmail('kanumuri1994@gmail.com')
        }
    }
  scriptPath(readFileFromWorkspace('pipeline.groovy'))
 }			
}
}
